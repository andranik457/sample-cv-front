<?php
{
//    $bucket = App::run('models/db/icon.php', ['object' => 'bucket'])['bucket'];
//    $downloadStream = fopen($_FILES['file']['tmp_name'], 'r');
//    $documentId = (string) $bucket->uploadFromStream($_FILES['file']['name'], $downloadStream, ['contentType' => $_FILES['file']['type']]);
//    fclose($downloadStream);

    var_dump($_POST);
    die('----------------------------');
}